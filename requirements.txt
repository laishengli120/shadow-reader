# ─────────────────────────────────────────────
# Web Framework
# ─────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0

# ─────────────────────────────────────────────
# OpenAI SDK（OpenAI / SiliconFlow 共用）
# ─────────────────────────────────────────────
openai>=1.30.0

# ─────────────────────────────────────────────
# HTTP 客户端（火山引擎、DashScope 音频下载等）
# ─────────────────────────────────────────────
requests>=2.31.0

# ─────────────────────────────────────────────
# 音频处理
# ─────────────────────────────────────────────
pydub>=0.25.1

# ─────────────────────────────────────────────
# 免费 TTS Provider
# ─────────────────────────────────────────────
edge-tts>=6.1.9          # EdgeTTSProvider — 微软 Edge Neural TTS，免费
gtts>=2.5.1              # GTTSProvider — Google Translate TTS，免费

# ─────────────────────────────────────────────
# 离线 TTS Provider
# ─────────────────────────────────────────────
pyttsx3>=2.90            # Pyttsx3Provider — 调用系统语音引擎，完全离线

# ─────────────────────────────────────────────
# 阿里云 DashScope（千问 TTS）
# ─────────────────────────────────────────────
dashscope>=1.19.0        # DashScopeProvider — 需阿里云百炼 API Key

# ─────────────────────────────────────────────
# 异步兼容（edge-tts 是 asyncio 库，在同步 Flask 中运行需要此包）
# ─────────────────────────────────────────────
nest-asyncio>=1.6.0

# ─────────────────────────────────────────────
# pydub 依赖的底层音频编解码器（非 Python 包，需系统安装）
# macOS:   brew install ffmpeg
# Ubuntu:  sudo apt install ffmpeg
# Windows: https://ffmpeg.org/download.html
# ─────────────────────────────────────────────